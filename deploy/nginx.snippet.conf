# Добавьте в server {} вашего nginx (тот, что обслуживает HTTPS-домен).
location /devbot/webhook {
    proxy_pass http://devbot:3002/webhook;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
}
